# 本机生产服务契约修复说明

重验证首先发现冻结策略与当前服务源码 SHA 不一致；随后发现 active confirmed 服务把生产 T2 错标为 H4，并把 `research_only` 默认置为 true。已在受控项目源码中修正，并用新 SHA 冻结 V3 策略后重新完成回环验收。

这次修复只改变服务元数据和解释文本，不改变模型权重、特征计算、概率或生产注册表；生产锚点仍为 `RG_OBGNET_CONFIRMED_SAFE_V1_1`。
