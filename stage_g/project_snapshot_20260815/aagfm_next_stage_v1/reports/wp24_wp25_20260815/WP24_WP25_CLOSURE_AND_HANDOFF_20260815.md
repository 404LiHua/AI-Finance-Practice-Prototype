# WP24/WP25 开发候选收尾与交接

日期：2026-08-15

## 结论

- WP24（共享主干 H4/T2 多任务）状态：`FAIL_DEVELOPMENT_JOINT_GATE_RESEARCH_ONLY`。
- WP25（生产锚定低幅残差 H4/T2）状态：`PASS_DEVELOPMENT_JOINT_GATE`，但仍是研究候选，未进入生产注册表。
- 当前生产内核保持 `RG_OBGNET_CONFIRMED_SAFE_V1_1`，生产目标保持 `T2_MARKET_RELATIVE_FIXED`。

## 证据摘要

WP24 的 H4 周 IC 六折均为正，但中位 MAE 相对零预测恶化 `0.0038812819`；相对 incumbent 的最差折 MCC 变化为 `-0.0320265`，Brier 最差增量为 `0.0159493`，因此关闭且不得在本协议内后验调参。

WP25 将冻结 incumbent 作为 T2 概率锚点，仅允许固定 `0.20` 的残差 logits 和有界 H4 残差。六折 H4 周 IC 均为正，中位 MAE 相对零预测改善 `0.0000982`；最差折 MCC 变化 `-0.0077008`，Brier 增量 `0.0035482`，通过开发联合门。

WP25 已通过 CPU 冷加载回放，连续输出最大差异约 `2.61e-8`，分类概率最大差异约 `1.19e-7`。

## 公开快照边界

本目录只公开协议、脚本、聚合指标、审计决定和非敏感制品元数据。密封预测表、训练权重、原始特征/标签和本地运行输出仍保留在项目工作区，不上传到公开仓库。

## 下一步

1. 补齐 CPU-only 单机 shadow 推理器并完成输入合同、非法输入、资源和回放验收。
2. 合法获取 WP25 所需 13 个 RG3 结构特征；缺失时不得广播、伪造或静默填补。
3. 待至少 12 个合格未来窗口和独立一次性授权后，先封存 incumbent/WP25 预测，再读取未来标签并一次性评分。
4. 未来独立评价通过且完成纸面交易与注册表评审前，不得替换生产 T2 内核。
