# 本机 T2 服务重验证 V4（2026-08-15）

V4 移除了 active confirmed `/api/model` 中过时的 V24 `formal_candidate` 元数据，并要求 `candidate_selection_status=NO_CANDIDATE_ACTIVE_PRODUCTION_RETAIN_ANCHOR`。回环验收再次通过目标契约、active 身份、research-only 标志、概率和、缓存、输入拒绝、图/事件分支关闭、GPU 禁用和服务关闭检查。

未读取标签/FRESH/WP10，未训练，未修改模型或注册表。
