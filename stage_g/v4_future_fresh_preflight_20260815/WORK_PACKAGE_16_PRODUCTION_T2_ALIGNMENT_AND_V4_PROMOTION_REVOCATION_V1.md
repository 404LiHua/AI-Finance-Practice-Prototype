# WP16：生产 T2 目标对齐与 V4 晋级撤销

状态：`FAIL_CLOSED / REQUIRED_BEFORE_ANY_PRODUCTION_COMPARISON`

## 结论

当前冻结候选 `AA_GFMNET_CROSS_SECTIONAL_NEUTRALIZED_RESIDUAL_TCN_V1` 的训练目标是 TextCU V2：周一 09:30 起点后第 4 个可用交易日的复权绝对收益。当前生产内核 `RG_OBGNET_CONFIRMED_SAFE_V1_1` 的目标是四个周度步长的市场相对收益，类别阈值为固定 `-1% / +1%`。

两者不是同一目标。候选的 T2 概率头不能把四交易日绝对标签自动变成生产四周相对 T2；连续收益头也不能绕过这个差异。任何直接比较 MCC、Brier、IC、纸面交易或晋级的结果都不具有生产语义效力。

因此 WP13、WP14、WP15 的 V4 构建器、绑定器和合成运行演练只保留为“标签无关工程链路验证”，不再授权真实 V4 标签物化、一次性评分、纸面交易或生产晋级。生产内核保持 `RG_OBGNET_CONFIRMED_SAFE_V1_1` 不变。

## 重新获得生产比较资格所需的资料

必须从原始归档恢复，或由等价 PIT 材料逐行重建并审计：

1. 生产 T2 的完整目标公式：四周起止点、复权/未复权口径、市场相对基准、截面成员和缺失处理；
2. 固定三分类阈值及其冻结证明；
3. 原始 `train.npz`、`rev8_ro01_train_target.csv.gz`、`rg3_features.csv.gz` 或可证明等价的来源及 SHA-256；
4. `STOCK_REGISTRY.csv`、`ORIGIN_REGISTRY.csv`、全市场 PIT 宇宙、上市/停复牌/ST/价格限制规则；
5. 生产训练、开发留出、独立 FRESH 与模拟盘的起点清单；
6. 与上述目标完全一致的四周标签、特征和可交易性审计。

不能用当前 300 股票短周期 H4 标签、当前日线临时拼接、已消费 FRESH、行业均值或猜测的市场指数替代这些资料。

## 后续正确路线

先完成 T2 语义恢复审计；若恢复成功，再新建一个“严格四周市场相对 T2”候选（模型可沿用提案结构，但制品身份、标签、训练和验证必须全部重新冻结），按 TRAIN-only → 开发留出 → 未触碰 FRESH → 独立模拟盘的顺序验证。任何阶段失败都关闭该候选，不影响现生产内核。

在 WP16 关闭前，禁止把现有 V4 结果写成生产增益或提交内核替换申请。

原子晋级器现已强制要求额外的 `--target-alignment-gate` 参数，且该收据必须为 `PASS_PRODUCTION_T2_TARGET_ALIGNMENT` 并绑定候选与生产锚点；当前 WP16 冲突收据为 FAIL，因此旧 V4 结果无法触发注册表写入。
