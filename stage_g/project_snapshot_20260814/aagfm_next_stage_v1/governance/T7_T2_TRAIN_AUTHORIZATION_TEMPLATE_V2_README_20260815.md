# T7 T2 授权模板 V2

这是旧 T7 H4 模板的不可覆盖修正版。V2 明确绑定生产 T2：四周市场相对收益、`origin_index + 4`、±1% 阈值和 5,513 股票键域。

V2 仍然不是授权：`status` 仍为 `REQUEST_TEMPLATE_NOT_AUTHORIZATION`，训练源与 SHA-256 仍为 `PENDING`，独立保管者签名为空。不允许读取标签、训练或修改生产内核。
