# 阶段 E：E-6.1 候选门槛冻结报告

日期：2026-07-25  
状态：`PASS / FROZEN BEFORE CANDIDATE ELIGIBILITY OR RANKING`  
本节点未读取候选指标、未计算候选资格、未生成排序或候选建议。

## 1. 冻结产物

配置：`stage_e/configs/e6_candidate_gate_v1.json`  
配置SHA-256：`7769f13af45110cb7f83f575193e352037150f4d3b48ec3beaab3d22c0f51f68`

来源诊断批次：`fea38e3c09ede4ab0c462bed89f5db87e8e0a1ffb04246b9f0d74acd792b3240`

基线角色固定为：

- Naive：`naive`；
- 强冻结基线：`frets_return_l4__fixed_shrink_a075`。

非基线候选池固定为现有8个模型，不允许新增模型。所有门槛为硬门槛，必须全部通过，不使用加权综合分。

## 2. 总体与最差折门槛

- 总体MAE必须相对Naive和FreTS分别至少改善0.5%；
- 解析后的总体MAE上限：`0.03174762995050519`；
- 总体RMSE不得超过更优基线的1.01倍，上限：`0.046445657668821305`；
- MAPE不作为晋级门槛；
- 最差折按“三种子在折内池化后MAE最大”定义；
- 最差折MAE不得超过更优基线的1.01倍，上限：`0.03538472706806972`；
- 三个折必须全部存在。

## 3. 逐股票、行业与市值门槛

逐股票：

- 必须覆盖100只股票；
- 至少70只股票的MAE低于Naive；
- 单只股票最大MAE不得超过 `0.08496014605505227`；
- 缺失任一股票即失败。

行业：

- 必须覆盖13个行业；
- 至少9个行业MAE不高于Naive；
- 每个行业MAE均不得同时超过Naive或FreTS对应值的1.05倍；
- 信息技术行业MAE上限：`0.04289315099005703`。

市值：

- 必须覆盖大、中、小三个市值组；
- 三组MAE均不得高于Naive；
- 每组均须位于两基线对应值的1.05倍以内；
- 中市值组MAE上限：`0.03446590249807528`。

## 4. 收益尾部门槛

- D1和D10必须同时存在；
- D1 MAE上限：`0.08514885348838286`；
- D10 MAE上限：`0.09260376891245437`；
- D1与D10平均MAE不得超过更优基线双尾平均的1.02倍，上限：`0.083646897100174`；
- 任一尾部缺失即失败。

## 5. 三种子稳定性门槛

- 三种子必须完整；
- 种子MAE变异系数不超过 `0.005`；
- 三组种子对Pearson均不低于 `0.80`；
- 三组种子对Spearman均不低于 `0.80`；
- 逐样本种子预测标准差均值不超过 `0.002`；
- 逐样本种子预测标准差P95不超过 `0.005`。

## 6. 工程成本与加载门槛

- 必须有9份“折×种子”工程回执；
- 总训练时间不超过180秒；
- 总运行时间不超过185秒；
- 参数量不超过200,000；
- 独立加载最大预测差不超过 `1e-7`；
- 必须有9份推理时间回执，合计推理时间不超过0.05秒；
- 推理成本证据缺失时，不得晋级，状态记为 `INELIGIBLE_UNTIL_INDEPENDENT_BENCHMARK_IS_FROZEN`。

## 7. 三种子推理聚合

- 固定种子顺序：`20260723/20260724/20260725`；
- 三种子等权算术平均，每个权重为1/3；
- 三个checkpoint必须独立加载；
- 样本键必须完全一致；
- 禁止事后校准、选择最佳种子或改用中位数；
- 不确定性输出固定为三种子逐样本预测标准差。

## 8. 唯一候选决议规则

- 0个模型通过：形成“无可晋级候选”的正式结果；
- 1个模型通过：形成唯一候选建议；
- 多个模型通过：按以下无权重字典序决议：最差折MAE、总体MAE、种子预测标准差均值、训练时间、模型ID；
- 禁止降低门槛或新增候选。

## 9. 验收与下一节点

机器冻结回执 `outputs/stage_e/e6_candidate_gate_freeze_receipt_v1.json` 通过13项检查：

- 候选指标读取：否；
- 候选排序：否；
- 候选资格评估：否；
- 候选建议形成：否；
- 新训练、未来数据和SCREENING授权：否。

门槛规则SHA-256：`1a31e0fcaf10c12eed360e30cafe06ead5d3a9ffdcec5331d274afce8b155996`  
三种子聚合SHA-256：`73d34785d90662ca392d8d4f60e61eb6b4caccc13afba2a172afc8550c597a82`  
冻结回执文件SHA-256：`88316f6ffa6ca542fec5e074f19957c5e7de9baf05fc968221c73708f8250b17`

下一节点只允许一次性读取现有8个候选模型的既有诊断行，逐项应用冻结硬门槛并形成唯一候选或正式无候选结论。不得训练、调参、补候选或修改门槛。
