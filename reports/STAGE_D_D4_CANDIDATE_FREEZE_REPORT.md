# 阶段 D-4：唯一候选与筛选规则冻结报告

冻结日期：2026-07-24

冻结 ID：`stage_d_frets_l4_shrink_a075_d4_20260724`
状态：`LOCKED_BEFORE_D_SCREENING`

## 1. 冻结对象

唯一候选：`frets_return_l4__fixed_shrink_a075`。

- 基础模型：FreTS return-only L4
- 特征顺序：仅 `return_1w`
- 序列长度：4
- 检查点来源折：`D_RO_03`
- 固定种子：`20260723`、`20260724`、`20260725`
- 每种子固定收缩：`prediction × 0.75`
- 三种子聚合：三个收缩后预测的等权算术平均
- 冻结基线：Naive，固定预测收益为 0
- 冻结后禁止重训、选择种子、改变权重或改变收缩系数

选择 `D_RO_03` 检查点是因为它是登记三折中时间最晚的开发折检查点；D-4 未使用 C-4 或未来 D-SCREENING 重新训练模型。

## 2. 总体 PASS 门槛

未来独立 SCREENING 必须同时满足：

- 候选 MAE 不高于 Naive。
- 候选 RMSE 不超过 Naive 的 1.05 倍。
- 方向准确率不低于 0.50。
- 方向 F1 不低于 0.15。
- 三种子 MAE 变异系数不超过 0.10。
- 30 只股票中至少 15 只的 MAE 不高于 Naive。
- 最差五只股票误差贡献不超过 45%。
- 任一股票相对 Naive 的 MAE 比值不超过 1.50。
- 负向尾部、正向尾部、近零收益组相对 Naive 的 MAE 比值分别不超过 1.05、1.10、1.50。

## 3. 性能 FAIL 条件

任一条件触发即为 `FAIL`：

- 候选 MAE 高于 Naive。
- 候选 RMSE 超过 Naive 的 1.10 倍。
- 方向准确率低于 0.45，或方向 F1 低于 0.10。
- 三种子 MAE 变异系数超过 0.15。
- 优于 Naive 的股票少于 12 只。
- 最差五只股票误差贡献超过 50%。
- 任一股票相对 Naive 的 MAE 比值超过 2.00。
- 负向尾部、正向尾部、近零收益组 MAE 比值分别超过 1.20、1.25、2.00。

没有触发 FAIL、但未通过全部 PASS 条件时，结果固定记录为 `INCONCLUSIVE`，不得在同一 SCREENING 数据上调参后重跑。

## 4. 完整性硬失败

以下情况返回 `INVALID_INTEGRITY_FAILURE`：冻结文件缺失或哈希不一致；检查点、上游源码、特征顺序、序列长度、种子、收缩或聚合方式不一致；候选和 Naive 评估行不同；股票覆盖不足；出现非有限预测；冻结后发生重训、阈值修改或候选变更；未授权读取 C-4 或未来 D-SCREENING。

## 5. 冻结包

冻结包复制并保管：

- 三个 `D_RO_03` FreTS L4 模型检查点及配置/指标/种子记录；
- 上游 `FreTS.py` 源码副本；
- 独立推理代码、统一评估器、判定策略代码；
- D-1 协议、数据封存规则、D-2 候选登记和 D-3 选择证据；
- 数据契约和 30 股票清单。

冻结清单包含 31 项文件。根 SHA-256：

`e3303c3b4818e547476b0bded9860c6fd0334cf1db4f5b4e5677249955d208f6`

## 6. 独立加载复算

冻结副本已在 `D_RO_03` 开发折完成独立加载与复算：

- 冻结包完整性验证：PASS
- 三个检查点独立加载：PASS
- 逐种子预测最大绝对差异：`1.2330×10⁻⁸`
- 三种子聚合最大绝对差异：`5.2517×10⁻⁹`
- C-4 读取数：0
- 未来 D-SCREENING 读取数：0

该开发复算按冻结规则得到 MAE `0.031268`，Naive MAE `0.031892`，但其结果仅证明工程闭环正确，不作为未来 SCREENING 证据。

## 7. 固定执行入口

验证冻结包：

```powershell
python -m stage_d.freeze_stage_d_candidate --verify
```

执行独立开发复算：

```powershell
python -m stage_d.run_d4_independent_recalc
```

未来 SCREENING 必须复用 `stage_d.inference.LoadedStageDFrozenCandidate` 和 `stage_d.d4_policy.evaluate_frozen_policy`，不得另写或修改评估逻辑。

## 8. D-4 结论

D-4 已完成。候选、Naive、三种子聚合、指标门槛、逐股票及收益分组失败条件、独立加载入口和 SHA-256 清单均已冻结。

未来 D-SCREENING 目前仍是未授权、未获取、未读取状态。下一步只能先申请明确授权；授权前不得查询覆盖、下载数据或运行筛选。
