# Stage E E-4.2 统一图频多模态适配器报告

报告日期：2026-07-25  
结论：`PASS / E-4.2 COMPLETED`

## 1. 适配目标

本适配器把E-3固定股票顺序和12周数值窗口，与E-2无文本、逐折TF-IDF/SVD、BGE语义编码统一为逐折横截面张量。所有文本只按冻结 `sample_row_id` 左连接，文本缺失不改变样本集合。

## 2. 输出结构

每折生成：

- `base_windows.npz`：`[cross_section,12,100,6]`数值窗口、目标、掩码、节点可用性、股票代码和样本键；
- `text_no_text.npz`：特征维度0；
- `text_tfidf_svd.npz`：特征维度32；
- `text_semantic_bge.npz`：特征维度512。

| 折 | TRAIN截面 | VALIDATION截面 | 样本行 | 有文本行 |
|---|---:|---:|---:|---:|
| E_RO_01 | 123 | 5 | 12,794 | 981 |
| E_RO_02 | 129 | 5 | 13,394 | 1,034 |
| E_RO_03 | 135 | 5 | 13,994 | 1,097 |

## 3. 泄漏控制

- 数值归一化只使用各折TRAIN窗口；
- TF-IDF/SVD直接加载E-2逐折TRAIN拟合产物，不重新拟合；
- 三个模型SHA-256均与E-2元数据一致；
- BGE为固定本地预训练编码器，适配阶段不训练；
- VALIDATION文本只转换和左连接，不进入任何拟合过程；
- 日期不超过2023-06-02；
- 未读取封存或未来数据。

## 4. 缺失文本处理

缺失文本节点使用零向量，但其数值窗口、目标、样本键和节点位置全部保留。`text_available=false` 和 `text_count=0` 继续进入模型，后续可用于显式缺失模态控制。

## 5. 验收

- 冻结样本行：40,182，全部保留；
- 三折样本ID与assignments完全一致；
- 三种视图样本矩阵与基础窗口完全同键；
- 所有缺失文本向量为零；
- 12个NPZ文件两次独立复算哈希一致；
- 批次 SHA-256：`a0a154551d40312734975597749fc52ef0971510f6c4c9185f6cf8cc660e9151`；
- 机器验收：`outputs/stage_e/e4_adapter_acceptance_100stocks_v1.json`，全部通过。

## 6. 下一步

E-4.3将在本批次上先运行100股票最小过拟合和三种子初检，再进入预登记的无图、固定图、动态图、无频域、频域、双分支和文本融合消融。

