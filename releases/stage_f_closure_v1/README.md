# Stage F closure archive

This package freezes the formal no-promotable-robust-candidate conclusion.
Retained model: `stock_node_gwnet_fixed_industry_l8`.

`source_snapshot/` contains necessary source/config/test/report files. `evidence_snapshot/` contains compact JSON/CSV receipts, including failed attempts. Checkpoints, NPZ arrays and row-level gzip predictions are intentionally excluded; their hashes remain recorded in receipts.
