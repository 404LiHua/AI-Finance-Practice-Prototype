# 阶段F极端行情与鲁棒性实施计划

计划版本：`stage_f_implementation_v1`  
启动日期：2026-07-28  
计划周期：2026-07-27 至 2026-09-20  
当前状态：`STAGE F CLOSED / NO ROBUST PROMOTABLE CANDIDATE / STAGE-E INCUMBENT RETAINED`

## 1. 不变边界

- 开发数据截止日固定为2023-06-02；
- 折固定为`E_RO_01/E_RO_02/E_RO_03`；
- 种子固定为`20260723/20260724/20260725`；
- 每个折×种子必须覆盖500个冻结验证样本；
- 不读取C-4、D-5、未来SCREENING或FINAL；
- 不改变100股票顺序、特征顺序、统一评估器和样本键；
- 失败模型、失败种子和负面结果不得删除。

## 2. 控制组

1. `naive`：最低基线；
2. `frets_return_l4__fixed_shrink_a075`：阶段D稳定控制；
3. `stock_node_gwnet_fixed_industry_l8`：阶段E唯一候选与鲁棒性锚点。

## 3. F-0协议冻结（已完成）

冻结内容包括9种正常/压力场景、3个有界非GAN候选、正常/压力/稳定性/工程硬门槛、数据路径拒绝和失败逻辑。GAN必须等待F-1完成并建立单独F-2附录。

机器验收11/11通过。F-0没有执行训练、读取候选指标、访问SCREENING或FINAL。

## 4. F-1有界非GAN稳健控制

### F-1.1 实现与最小测试

只允许实现以下三个候选：

- `stock_node_gwnet_tail_weighted_l8`：TRAIN内绝对目标收益Q90尾部权重2；
- `stock_node_gwnet_noise_aug_l8`：TRAIN数值特征确定性高斯噪声，sigma=0.03；
- `stock_node_gwnet_feature_mask_l8`：TRAIN数值特征确定性5%掩码。

先完成张量形状、确定性、无验证拟合和最小过拟合测试。

完成状态：2026-07-28已完成。三个候选均保持阶段E固定行业图L8架构不变，只修改冻结的训练损失或TRAIN数值输入。确定性、张量形状、三候选合成最小过拟合、TRAIN-only阈值拟合及封存路径拒绝共4类检查全部通过；没有运行正式折训练、读取候选指标或访问未来数据。

### F-1.2 单种子工程回执

使用种子`20260725`运行三折，验证训练损失有限、checkpoint独立加载、样本键一致、正常与压力推理可运行且未读取未来路径。本节点不排名、不删模、不形成候选建议。

完成状态：2026-07-28已完成。最终有效批次9/9运行、12/12机器验收通过，形成4,500行冻结验证预测和9个独立检查点。首轮因把场景非空错误实现为逐折检查而形成的FAIL批次已完整保留；修正仅恢复三折汇总非空语义，没有改变训练范围。没有运行另外两个种子或形成候选排序。

### F-1.3 三种子复核

保持协议和代码SHA不变，补运行`20260723/20260724`，形成3候选×3折×3种子的27个回执。禁止删除失败种子或只报告最佳种子。

完成状态：2026-07-28已完成。新增18/18运行和三种子27/27工程回执通过，13,500行预测、冻结样本键、独立加载及压力入口均通过。三个候选的MAE CV和预测方差门槛通过，但最低Pearson/Spearman均未达到0.80，因此完整稳定性硬门槛均为FAIL；未删除失败种子、放宽门槛或进行候选排名。

### F-1.4 统一鲁棒性诊断

合并控制组和三个候选，输出正常误差、8个压力场景、压力综合MAE、逐股票/行业/市值/尾部、三种子稳定性和工程成本。严格应用F-0硬门槛，结果只能是唯一非GAN鲁棒候选或正式无可晋级候选。

完成状态：2026-07-28已完成。统一复核27,000行正常预测和114,696行压力预测，三个候选分别通过12/20、13/20和13/20项硬门槛，均保留Pearson/Spearman失败且压力改善门槛不足。正式结论为无可晋级稳健候选，继续保留阶段E固定行业图模型；没有排名或放宽门槛。

## 5. F-2 GAN边界

F-0和本计划不授权GAN训练。只有F-1完整诊断结束后，才允许建立F-2附录，冻结唯一生成器/判别器结构、损失、预训练、梯度惩罚、谱归一化、崩溃条件和成本上限。

## F-2.0 GAN附录冻结（已完成）

- 唯一候选固定为`stock_node_gwnet_bounded_cwgan_gp_l8`，不允许第二个GAN结构。
- 生成器、判别器、WGAN-GP损失、5+20 epoch计划、3:1更新比、归一化、13项坍塌失败条件及360秒九次运行成本上限均已冻结。
- 契约测试3/3、冻结检查14/14、独立验收12/12通过。
- 本节点没有执行优化器步骤、GAN训练、SCREENING或FINAL访问。
- F-2.1最小训练健康检查已获得授权并完成。

## F-2.1 最小GAN训练健康检查（已完成）

- 使用种子`20260725`和`2×8×100×6`合成小批次，严格复用冻结的5+20轮、3:1更新比及Adam参数。
- 确定性、形状与有限性、最小过拟合/参数更新、数据边界4/4通过；协议检查9/9、独立验收10/10通过。
- 正式三折训练、额外种子、候选指标读取、排名、SCREENING和FINAL均未执行。
- 生成器复合损失因Critic同步更新不可直接跨时点比较，F-2.1不形成性能或晋级结论。

## F-2.2 单种子三折GAN工程回执（已完成）

- 仅使用种子`20260725`运行冻结三折，三折GAN损失、预测器损失和全部坍塌条件通过。
- 三折检查点独立加载差异均低于`1e-7`，冻结VALIDATION样本键共1,500行一致，正常与9类压力入口可运行。
- V1空增强批次适配器失败与V2逐折成本失败均保留；V3只优化重复检查点加载，未改变训练协议或成本门槛。
- V3逐折耗时29.290/30.745/35.543秒，三折总耗时95.608秒，独立验收10/10通过。
- 本节点不形成性能排名或晋级建议；种子`20260723/20260724`仍未授权。

## F-2.3 三种子GAN复核（已完成）

- 在V3训练代码和全部冻结协议不变的前提下补运行种子`20260723/20260724`，新增6个运行全部工程通过。
- 合并种子`20260725`后形成9/9回执、4,500行冻结预测、9次总耗时244.316秒，工程独立验收11/11通过。
- 稳定性仅MAE CV通过；最低Pearson 0.3212、Spearman 0.1840、预测标准差均值0.002452和P95 0.007111均失败。
- 正式状态为`ENGINEERING_PASS_STABILITY_HARD_GATE_FAIL`，不得由工程PASS或MAE接近抵消。
- 未排名、删种子、形成晋级建议或访问SCREENING/FINAL。

## F-2.4 统一鲁棒性诊断（已完成）

- 离线合并3个控制组、3个F-1候选和1个GAN候选，共31,500行正常预测、133,812行压力预测；没有重新训练或执行新模型推理。
- 四个候选分别通过12/20、13/20、13/20和11/20项硬门槛；GAN正常性能与工程门槛通过，但压力门槛不足且四项稳定性硬失败原样保留。
- 独立验收11/11通过，结论为`FORMAL_NO_ROBUST_PROMOTABLE_CANDIDATE_RETAIN_STAGE_E_INCUMBENT`。
- 未排名、删模、改阈值或访问SCREENING/FINAL。

## F-3 阶段F正式收尾与冻结（已完成）

- 生成阶段F最终报告，冻结四个候选的20项门槛结果和正式无可晋级结论；
- 冻结GAN四项不可补偿稳定性失败，并继续保留阶段E incumbent；
- 建立`releases/stage_f_closure_v1`，归档阶段F必要源码、配置、测试、报告及紧凑证据；
- 生成完整SHA-256 JSON/CSV清单、冻结回执和独立验收入口；
- 大体量检查点、NPZ及逐行gzip预测不进入Git，但其哈希和运行回执继续保留。

## 6. 阶段关闭与下一节点

阶段F正式关闭。下一节点为`G-0证据化解释协议冻结`：在任何LLM训练或解释候选比较前，冻结证据字段、点时边界、事实正确性、证据覆盖、拒答、风险提示、自动评价和人工双评规则。未经另行授权，不读取新的SCREENING或FINAL。
