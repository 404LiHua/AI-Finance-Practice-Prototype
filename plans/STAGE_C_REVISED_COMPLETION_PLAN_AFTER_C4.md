# 阶段 C 独立 SCREENING 后修订完成计划

更新日期：2026-07-24  
当前状态：C-1 至 C-4 已完成，独立 SCREENING 判定为 `FAIL`

## 1. 已完成节点

| 节点 | 状态 | 完成内容 |
|---|---|---|
| C-1 | 已完成 | 推荐 v2 的独立模型加载、三种子复算和推理闭环 |
| C-2 | 已完成 | 工程成本、逐股票误差、收益分组误差和组件分歧诊断 |
| C-3 | 已完成 | 候选、六组基线、指标门槛、失败条件和 174 项 SHA-256 清单冻结 |
| C-4 | 已完成：FAIL | 30 股票、1,500 样本、一次性未来 SCREENING；候选 MAE 差于 Naive |

## 2. C-4 对阶段结论的影响

推荐 v2 仍可作为阶段 C 的工程研究成果和稳定结构对照，但不能标记为“独立泛化验证通过”。阶段 C 的准确结论调整为：

```text
工程实现完成
+ 开发期模型研究完成
+ 候选与规则冻结完成
+ 独立 SCREENING 完成
- 独立性能验收未通过
```

不得使用本次 2023-06-09 至 2024-06-07 SCREENING 数据继续修改推荐 v2，也不得在同一区间为新候选重复声明独立评估。

## 3. 剩余 C-5 收尾任务

### C-5.1 阶段总结

- 将 C-4 的 `FAIL` 结论写入阶段 C 总报告和项目 README。
- 清楚区分开发期 validation 结果与独立 SCREENING 结果。
- 保留推荐 v2 的工程价值、稳定性优点和失败原因，不夸大泛化能力。

### C-5.2 证据与版本归档

- 保存 C-3 冻结清单、C-4 数据清单、判定文件、逐种子指标和压缩预测明细。
- 为阶段 C 源码、配置、报告和必要证据生成最终归档 SHA-256 清单。
- 更新 `D:\项目\源文件\importance`，排除不应传播的受限原始数据。

### C-5.3 Git 与 GitHub

- 检查并整理阶段 C 必须源文件和说明文档。
- 提交阶段 C 最终版本并建立明确标签。
- 推送 GitHub，确保仓库不包含原始受限数据、环境目录或大体积模型输出。

## 4. 后续研究边界

如果需要继续改进模型，应进入新的后续研究阶段，不属于 C-4 修补：

- 新候选只能使用训练/开发数据和本次 SCREENING 的汇总失败结论，不能针对 1,500 条逐样本误差调参。
- 新阶段必须提前冻结新的候选和门槛。
- 必须预留另一段从未读取的未来数据作为新的独立 SCREENING。
- 在新的独立证据产生前，不得宣称推荐 v2 或后续模型已通过泛化验收。

## 5. 下一步

立即执行 C-5：完成阶段 C 总报告、版本归档、Git 提交/标签、GitHub 推送和 `importance` 更新。阶段 C 的最终验收结论应保留为“工程完成，独立性能验收未通过”。
