# 阶段 E 后续实施计划 V2

计划版本：`stage_e_revised_execution_v2`  
制定日期：2026-07-25  
计划周期：2026-07-27 至 2026-09-20  
当前状态：`E-1 PASS / E-2 PASS / E-3 PASS / E-4 CLOSED AS NEGATIVE RESULT / E-5.1 INTERFACE PASS / E-5.2 NEXT / E-6 NOT STARTED`

## 1. 重排原因

E-4已经完成架构、适配、两层消融、固定图复核、控制组复核和分组诊断，但学习动态图、固定行业图+BGE mid、无图时序和固定行业时域控制均未同时通过原三种子稳定门槛。问题表现为平均MAE对种子稳定，但逐样本预测相关不足，当前没有可扩展至300股票的稳定E-4模型。

因此后续不直接进入300股票图频/文本融合，也不读取未来SCREENING。阶段E改为“E-4S稳定化主线 + E-5统一基线主线”并行推进，最终在E-6汇合。

## 2. 不变边界

- 开发截止日保持 `2023-06-02`；
- rolling-origin保持 `E_RO_01` 至 `E_RO_03`；
- 原三种子保持 `20260723/20260724/20260725`；
- E-4稳定门槛保持：边Jaccard≥0.04、预测相关≥0.90、MAE变异系数≤0.50；
- E-3股票顺序、Top-k=8、26周相关窗口和图批次不修改；
- 文本继续使用E-2冻结的无文本、TRAIN-only TF-IDF/SVD和BGE视图；
- 不读取C-4、D-5或未来Stage E SCREENING/FINAL；
- Stage D冻结模型 `frets_return_l4__fixed_shrink_a075` 保持不变。

## 3. 主线一：E-4S稳定化协议V2

### E-4S.1 稳定性指标审计

只做只读诊断，不重新训练：

- 复核三种子预测的样本键、顺序、掩码和反归一化；
- 分折、逐股票计算Pearson、Spearman、方向一致率和预测方差；
- 检查低预测方差是否导致相关系数不稳定；
- 对比训练曲线、最佳epoch、参数范数和预测幅度；
- 形成“不稳定来自实现错误、评价定义还是训练收敛”的唯一审计结论。

验收：冻结样本键完全一致；相关指标独立复算一致；不得根据审计结果新增模型候选。

### E-4S.2 两个控制组的单一训练协议V2

仅允许原控制组：

1. `temporal_no_graph_no_text`；
2. `time_industry_no_text`。

拟冻结的训练协议：

- 使用每折全部TRAIN截面，不再截取最近48个；
- hidden维度保持16；
- 固定40个epoch，不使用VALIDATION选择最佳epoch；
- AdamW，学习率0.001，weight decay 0.0001；
- cosine学习率调度、梯度裁剪5.0；
- 使用训练期EMA权重，衰减0.99，最终只评估固定末轮EMA；
- 三个种子仅改变初始化和TRAIN批次顺序；
- 所有数值在首次运行前写入独立配置并生成SHA-256，看到结果后不得修改。

门槛A：至少一个控制组通过原三种子全部门槛。

- 通过：进入E-4S.3；
- 失败：E-4永久关闭为负结果，不运行300股票复杂模型，后续只推进E-5基线和申报研究结论。

### E-4S.3 唯一图频文本结构复核

门槛A通过后，只允许复核既有唯一结构：

`dual_industry_fixed_bge_mid`

该结构必须使用与稳定控制组完全相同的训练协议V2，不增加新图、新融合、新文本位置或超参数分支。

门槛B：原三种子稳定门槛全部通过，且相对通过门槛A的控制组在平均MAE或方向准确率上至少一项改善，同时最差折不得恶化超过预冻结容忍度。

- 通过：进入E-4S.4；
- 失败：保留稳定控制组，E-4图频文本候选终止。

### E-4S.4 300股票扩展

只在门槛B通过后执行：

- 建立300股票E-4统一适配器，保持冻结样本键和TRAIN-only文本变换；
- 先运行前向、反向、显存/内存和产物落盘检查；
- 再运行三折三种子；
- 输出逐股票、行业、市值、收益分组、最差折、文本覆盖和工程成本；
- 100与300股票使用相同结构和训练协议，不允许重新调参。

门槛C：300股票运行完整、三种子稳定、分组风险不触发预登记失败条件。只有通过门槛C，E-4才可由 `NOT COMPLETED` 改为 `COMPLETED`。

## 4. 主线二：E-5统一基线与评价

E-5工程建设不依赖E-4S是否成功，可以在E-4S.1后开始。

### E-5.1 冻结统一接口

- 统一数值/文本视图、rolling-origin折、样本键和掩码；
- 统一训练日志、随机种子、checkpoint、预测文件和独立加载入口；
- 统一回归、方向分类、逐股票和分组评价器；
- 增加未来路径和封存数据拒绝检查。

验收：同一模型重复加载复算指标一致；所有模型读取相同VALIDATION行集合。

### E-5.2 第一批低成本基线

- Naive；
- Stage D冻结FreTS L4；
- 价格版Minimalist Transformer；
- RF；
- SVM；
- VAR或行业因子VAR。

先完成单种子工程回执，再按统一协议运行三种子。不得因结果较差删除已登记基线。

### E-5.3 第二批神经网络与图基线

- LSTM；
- TCN；
- Time-GNN稳定分支；
- 一个固定实现的MTGNN或Graph WaveNet类基线。

图基线必须使用真实股票节点，不得重新引入时间位置图。外部代码需要登记版本、许可证、源码SHA-256和适配改动。

### E-5.4 统一诊断

所有模型统一输出：

- MSE、MAE、RMSE、MAPE；
- Accuracy、Precision、Recall、F1、MCC；
- 三种子均值、标准差和预测相关；
- 最差折；
- 逐股票、行业、市值、收益十分位；
- 训练时间、推理时间、参数量、峰值内存；
- 模型间预测分歧。

## 5. E-6候选建议与冻结

E-6只在E-5完整基线完成后启动。

### E-6.1 门槛冻结

在读取E-5最终汇总排名前，冻结：

- 相对Naive和Stage D冻结FreTS的最低改善要求；
- 最差折容忍度；
- 三种子稳定性要求；
- 逐股票胜率、行业覆盖和收益尾部失败条件；
- 工程成本上限；
- 三种子推理聚合方式；
- 独立加载复算入口和SHA-256清单。

### E-6.2 唯一建议

- 若E-4S通过门槛C：E-4候选与完整基线统一比较；
- 若E-4S失败：不得将E-4开发最优结果晋级，只能从通过稳健门槛的E-5基线中形成建议；
- 若没有模型通过：形成“阶段E无可晋级候选”的正式负结果，不降低门槛。

### E-6.3 SCREENING授权

只有唯一候选、基线、指标门槛、失败条件、推理聚合方式和SHA-256全部冻结后，才申请一次未来E-SCREENING授权。授权前不得获取或探查未来数据范围。

## 6. 八周安排

| 周次 | 日期 | 任务 | 退出物/门槛 |
|---|---|---|---|
| 第1周 | 07-27至08-02 | E-4S.1指标审计；冻结训练协议V2；E-5.1接口设计 | 审计报告、冻结配置、接口契约 |
| 第2周 | 08-03至08-09 | E-4S.2两个控制组三种子；E-5.1实现 | 门槛A；统一适配与评估器 |
| 第3周 | 08-10至08-16 | 若A通过则E-4S.3；并运行E-5.2低成本基线 | 门槛B；第一批基线回执 |
| 第4周 | 08-17至08-23 | 若B通过则300股票工程检查；否则关闭E-4 | 300规模回执或E-4负结果 |
| 第5周 | 08-24至08-30 | 300股票三折首轮或继续E-5.2三种子 | 门槛C初检、低成本基线汇总 |
| 第6周 | 08-31至09-06 | E-5.3神经网络与图基线 | 第二批基线回执 |
| 第7周 | 09-07至09-13 | E-5.4统一分组、成本和分歧诊断 | 完整统一汇总器与诊断报告 |
| 第8周 | 09-14至09-20 | E-6门槛冻结、唯一建议、独立复算 | 冻结清单；决定是否申请SCREENING |

门槛失败会缩短E-4路径，但不会取消E-5统一基线工作。任何延期不得通过减少种子、移动折边界或读取未来数据弥补。

## 7. 当前最优先任务

下一执行节点固定为 `E-4S.1稳定性指标审计`，随后在任何新训练开始前生成：

1. `e4_stability_audit_v1`配置；
2. 样本键与三种子预测独立复算回执；
3. 分折/逐股票相关与预测方差报告；
4. E-4S训练协议V2配置及SHA-256；
5. E-5统一接口详细设计草案。

在这些产物完成前，不运行新的E-4训练，不启动300股票图频融合，也不申请SCREENING。

## 8. E-4S.1完成回执

`e4_stability_audit_v1`已通过机器验收：

- 三种子样本键和目标哈希一致；
- 学习动态图原指标独立复算差异为0；
- 所有预测方差非零；
- 验证结构最低两两Pearson为0.25557；
- 控制组最佳epoch范围1至12，固定图结构范围1至9；
- 唯一结论为训练收敛与VALIDATION checkpoint选择不稳定。

训练协议V2已在首次新训练前冻结，SHA-256为 `dcaf3b6899296939b06859b43eb0e26b40b5f0556fd8d89867231b5bb4b125ef`。E-5统一接口详细草案已生成。下一执行节点更新为E-4S.2两个既有控制组的V2协议三种子复核；300股票与SCREENING仍保持禁止。

## 9. E-4S.2完成回执

两个控制组已按冻结V2协议完成三折三种子共18次运行。无图时序最低预测相关从0.39088提高至0.76792，固定行业时域图从0.25557提高至0.73864；两组MAE变异系数分别为0.00435和0.00411。

尽管稳定性显著改善，两组仍未达到0.90预测相关门槛，因此门槛A为 `FAIL`，没有稳定控制组。按照本计划预先登记的失败路径：

- 不运行E-4S.3；
- 不运行300股票E-4图频/文本融合；
- 不继续修改训练协议V2；
- E-4正式关闭为负结果；
- 下一节点切换为E-5.1统一接口实现与机器验收。

E-4S.2批次 SHA-256为 `4522176dd51a8fac2d37cce6fdaab5d1e5de120ddc98e5efead34cba3a8ce270`。

## 10. E-5.1完成回执

E-5统一折视图、模型适配抽象类、预测文件契约、独立评价器和分组诊断器已经实现并通过无训练机器验收。两个既有控制组的9000行预测全部精确匹配冻结验证键；三折三种文本视图的样本键一致；全部指标独立复算哈希一致。

机器回执为 `outputs/stage_e/e5_interface_acceptance_v1.json`，接口批次 SHA-256为 `7013461b447d04fb7469fc4f1802a4c7aadb6b5c03d8defadc9b9dcc91d81f55`。下一节点为E-5.2第一批低成本基线的运行前登记和单种子工程回执。

### E-5.2运行前登记

第一批模型冻结为：Naive、`frets_return_l4__fixed_shrink_a075`、`minimalist_price_only_l8`、`random_forest_price_l12`、`svm_rbf_price_l12`、`industry_var1_ridge`。工程种子固定为20260725，覆盖全部三折。

FreTS与Minimalist Transformer沿用Stage D登记结构；RF、SVM和无statsmodels依赖的封闭式行业VAR(1)+ridge使用唯一配置，不做网格搜索。所有模型必须完成checkpoint落盘、独立加载复算和统一预测契约。单种子结果仅为工程暴露，不允许排名、删模或晋级。

## 11. E-5.2 单种子工程回执

第一批六个低成本基线已按冻结配置完成三折单种子工程运行，共18次，全部通过；每个模型覆盖1500个冻结验证键，缺失键和额外键均为0，checkpoint独立加载最大预测差均为0。机器验收 `outputs/stage_e/e5_low_cost_single_seed_acceptance_v1.json` 状态为 `PASS`。

本批次只标记为 `TRAIN/VALIDATION single-seed engineering exposure`，没有模型排名、删除或候选晋级，也未访问未来或SCREENING数据。下一执行节点为保持原模型集合、特征视图和训练协议不变，补运行种子 `20260723/20260724` 并形成三种子统一汇总；三种子完成前不进行候选选择。

## 12. E-5.2 三种子统一复核完成

已保持第一批六模型、特征视图、三折和训练协议不变，补运行种子 `20260723/20260724`，并复用原验收种子 `20260725`。新增36次运行全部通过，三种子合计54份回执、0失败；所有预测键、独立加载、指标复算和SHA检查均通过，机器验收为 `outputs/stage_e/e5_low_cost_three_seed_acceptance_v1.json`。

稳定性诊断显示确定性模型完全一致，RF预测相关较高，FreTS和Minimalist Transformer存在预测级种子差异；本节点没有执行排名、删模或候选晋级。下一执行节点更新为E-5.3第二批神经网络与真实股票节点图基线的运行前冻结和单种子工程回执。E-5.4统一诊断、E-6门槛冻结和SCREENING仍未启动。

## 13. E-5.3 冻结与单种子工程回执

第二批模型已经冻结为LSTM、TCN、确定性Top-k Time-GNN和固定行业图Graph-WaveNet类股票节点基线。Time-GNN上游MIT许可及源码SHA已登记，硬Gumbel采样已在运行前固定替换为确定性Top-k；真实股票图固定使用100个 `stock_code` 节点和同行业行归一化邻接。

工程种子 `20260725` 已覆盖三折，共12次运行全部通过。独立加载差、冻结样本键、Time-GNN确定性图检查、股票节点顺序/邻接SHA、指标复算和封存拒绝均通过机器验收 `outputs/stage_e/e5_neural_graph_single_seed_acceptance_v1.json`。

当前仍只属于单种子工程暴露，没有模型排名或候选晋级。下一执行节点为保持配置SHA不变，补运行 `20260723/20260724` 并形成E-5.3三种子汇总；之后进入E-5.4跨第一、第二批基线的统一成本、最差折、逐股票、行业、市值、收益分组和组件分歧诊断。

## 14. E-5.3 三种子统一复核完成

已在基础配置SHA不变的条件下补运行 `20260723/20260724`，并复用原种子 `20260725`。三折、四模型、三种子共36份回执全部通过；9份Time-GNN回执保持确定性Top-k，9份真实股票图登记保持相同100股票顺序和固定行业邻接。

三种子指标、独立加载、预测契约、股票图登记和SHA复算均通过机器验收 `outputs/stage_e/e5_neural_graph_three_seed_acceptance_v1.json`。稳定性结果仅作描述，没有进行排名、删模或候选晋级。

下一执行节点更新为E-5.4统一诊断。在任何跨模型比较开始前，先冻结E-5.2与E-5.3共10个模型的汇总集合、成本字段、最差折、逐股票/行业/市值/收益分组、种子稳定性、组件分歧和失败条件；E-5.4只读取既有TRAIN/VALIDATION预测，不重新训练，不访问SCREENING。

## 15. E-5.4 十模型统一诊断完成

E-5.4诊断规则已在跨批次读取前冻结，随后只读合并E-5.2六模型和E-5.3四模型的45,000行三种子预测及90份工程回执。成本、池化折、最差折、逐股票、行业、市值、收益十分位、种子稳定性/离散度和45组模型分歧全部生成，机器验收 `outputs/stage_e/e5_unified_diagnostics_acceptance_v1.json` 为 `PASS`。

统一诊断确认：10个模型的共同最差折均为E_RO_03；信息技术行业是共同最高误差行业；多数模型在中市值组误差最高；收益尾部D1/D10必须同时进入失败条件；种子平均MAE稳定不能替代逐样本预测稳定性；工程成本跨度显著。

本节点没有重新训练、写checkpoint、排名、删模或形成候选。下一执行节点更新为E-6.1候选门槛冻结：必须在读取任何候选排序前冻结相对Naive/FreTS改善、最差折、逐股票覆盖、行业/市值/收益尾部、种子稳定性、成本上限、三种子聚合和独立加载要求。门槛冻结前不得形成唯一候选或申请SCREENING。

## 16. E-6.1 候选门槛已冻结

E-6.1已在读取候选指标和排序前冻结。门槛同时覆盖相对Naive/FreTS总体改善、最差折、100只股票覆盖、13行业、三市值组、D1/D10双尾、三种子相关与逐样本方差、工程成本、独立加载和三种子等权聚合。

全部门槛为硬通过制，不使用加权分数。多模型同时通过时，仅允许按预冻结的最差折MAE、总体MAE、种子预测方差、训练成本和模型ID顺序决议。冻结回执 `outputs/stage_e/e6_candidate_gate_freeze_receipt_v1.json` 明确记录候选指标未读取、资格未计算、排序未执行。

下一执行节点为一次性应用冻结门槛：只读取8个既有非基线模型的E-5.4诊断行，不训练、不调参、不新增模型。输出只能是唯一候选建议或正式无可晋级候选结论；完成前不申请SCREENING。

## 17. E-6.2一次性审查与阶段收尾完成

2026-07-25已消费唯一一次候选诊断读取授权。8个非基线模型中仅`stock_node_gwnet_fixed_industry_l8`通过全部34项冻结硬门槛，正式状态为`UNIQUE_CANDIDATE_RECOMMENDATION`；未触发排序或并列裁决，未重新训练、修改阈值、新增候选或访问SCREENING。机器验收13/13通过。

2026-07-26完成E阶段收尾：生成最终报告、项目至今总结、最佳模型三折×三种子独立交付包、SHA-256清单和真实封存指标图形化看板。E阶段状态更新为`CLOSED / UNIQUE DEVELOPMENT CANDIDATE FORMED`。任何后续训练、候选扩展或独立数据评估必须建立新阶段协议，不得覆盖本阶段一次性结论。
