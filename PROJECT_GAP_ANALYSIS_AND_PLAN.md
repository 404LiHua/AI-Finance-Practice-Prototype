# 项目申报书与当前成果差距分析及修订计划

分析日期：2026-07-24
申报周期：2026-06 至 2027-06
申报项目：基于多模态融合与自适应图学习的金融时序预测模型研究
对比版本：申报书原件与 Git 提交 `d6a3372`

## 一、结论摘要

当前项目已经完成真实数据管线、统一训练评估、多个基线、图频结构消融、三种子与滚动验证、模型冻结、独立加载复算以及两次受控未来区间评估。就“研究工程基础”而言，进度已经明显超过申报书第 1—2 月的计划，并提前完成了部分第 3—4 月和第 9—10 月才安排的实验治理工作。

但按照申报书完整承诺重新核对，当前成果不能认定为项目主体已经完成，原因有四项：

1. 申报书要求以股票、板块或经济变量作为图节点，学习随时间变化的横截面依赖；当前阶段 C 图模型主要在单股票样本的时间位置之间建图，尚未实现真正的股票横截面动态图。
2. 当前独立验证通过的唯一候选是 `frets_return_l4__fixed_shrink_a075`，它是稳健的频域基线和工程控制组，不是申报书提出的“多模态动态图频融合主模型”。
3. GAN 鲁棒性增强、LLM 自我反思与 PPO、解释质量评价、投资绩效和交易成本评价尚未实现。
4. 当前系统仍以离线研究流水线和静态页面为主，缺少合法商用数据授权、生产 API、权限安全、在线监控、SLA、灰度发布和合规验收。

基于仓库可核验证据进行区间估算：

- 研究工程基础完成度：约 70%—80%；
- 申报书全部技术与成果承诺完成度：约 40%—45%；
- 可商用验收准备度：约 15%—20%。

这些比例是任务权重估算，不是学校正式验收结论。当前时间仍处于申报周期第 2 月，总体时间没有落后，但必须立即把研发重点从“继续优化单一 FreTS 候选”转回申报书的横截面动态图、多模态、鲁棒性和解释主线。

## 二、申报书主要承诺

申报书承诺的主体不是单一预测模型，而是以下完整系统：

1. 建立多源异构金融数据集，按时间 70%/10%/20% 划分，并使用滚动窗口验证。
2. 以股票、板块或经济指标作为节点，通过 Gumbel-Softmax 和 Top-k 学习动态图。
3. 采用“时域编码—FFT—频域图传播—IFFT—预测”的图频融合路线。
4. 构建 ACNN-LSTM/图频生成器和 CNN 判别器，引入梯度惩罚、谱归一化和多任务损失。
5. 融合数值和文本模态，既做输入层早期融合，也做频域后的中期融合。
6. 让 LLM 读取时序、图结构和文本证据，形成自我反思数据，并使用 PPO 和 KL 约束优化解释。
7. 对比 ARIMA、VAR、SVM、RF、LSTM、TCN、Transformer、MTGNN、Graph WaveNet、TimeGNN、FreTS、TSGAN、SEP 等模型。
8. 评价 MSE、MAE、RMSE、MAPE、Accuracy、Precision、Recall、F1、MCC、年化收益、夏普比率、最大回撤和解释质量。
9. 完成模块消融、端到端系统、论文、软件著作权或专利以及最终成果申报。

## 三、当前已完成内容与证据

| 能力 | 当前证据 | 结论 |
|---|---|---|
| 真实数据与追溯 | `data_pipeline/`、`reports/STAGE_A_30_STOCKS_REPORT.md` | 已完成 30 股票一年周频双源价格、CSMAR 事件、交易周历、哈希追溯和无泄漏切分 |
| 文本特征初版 | `build_text_features.py`、`text_features_metadata.json` | 已实现 TRAIN-only TF-IDF、SVD、KMeans，但仅 18 条特殊处理/股本事件，不能代表通用财经文本模态 |
| 统一实验框架 | `experiments/`、`reports/STAGE_B_ACCEPTANCE_REPORT.md` | 已完成配置、随机种子、日志、训练、评估、权重和预测产物统一管理 |
| 首批基线 | Naive、移动平均、ARIMA、LSTM、Minimalist Transformer、FreTS、Time-GNN | 已完成 7 模型 × 3 种子；缺少申报书列出的多项横截面、机器学习、GAN 和解释基线 |
| 图频模型原型 | `stage_c/models/` | 已实现动态图、确定性 Top-k、固定图、FFT 图传播、时域图和融合消融 |
| 稳定性诊断 | 阶段 C 报告 | 已确认随机 Gumbel、频域传播和可学习融合存在不稳定或性能损失，并保留失败证据 |
| 滚动验证与数据封存 | `stage_d/rolling_origin.py`、`custody.py` | 已完成三折 rolling-origin、未来数据拒绝检查和统一汇总 |
| 冻结与独立复算 | `stage_d/frozen/`、`stage_d/inference.py` | 已完成三种子检查点、独立加载、SHA-256 清单和聚合复算 |
| 未来独立评估 | `reports/STAGE_D_D5_INDEPENDENT_SCREENING_REPORT.md` | 冻结 FreTS L4 收缩候选相对 Naive 的 MAE 改善 0.501%，判定 PASS |
| 工程诊断 | 阶段 C/D 报告 | 已完成逐股票、收益分组、最差折、种子稳定性、组件分歧、耗时和参数量诊断 |
| 前端与说明 | `dashboard.html`、`README.md`、`docs/` | 已完成离线预览和项目说明，但页面仍使用内嵌样例数据，不是生产系统 |

## 四、逐项差距矩阵

| 申报任务 | 当前状态 | 关键差距 | 优先级 |
|---|---|---|---|
| 多源异构金融数据 | 部分完成 | 仅 30 股票、一年周频；缺少足量新闻、公告、研报或情绪文本，缺少长期和多市场状态覆盖 | P0 |
| 商业数据授权 | 未完成 | 九章、BaoStock、CSMAR 当前用途不能自动推定为商业许可；CSMAR 导出明确受校内使用限制 | P0 |
| 股票横截面动态图 | 未完成 | 当前邻接矩阵维度主要是时间位置 `[B,T,T]`，不是股票/变量节点 `[B,N,N]` | P0 |
| 图频核心创新 | 原型完成、证据不足 | FFT 图传播已实现，但在现有单股票短序列上未形成稳定优势，需在横截面节点设计上重新验证 | P0 |
| 多模态融合 | 初版完成 | 只有稀疏事件 TF-IDF/SVD；缺少语义编码、发布时间点时处理、早期/中期融合和模态缺失机制 | P0 |
| 稳健候选 | 基线候选通过 | FreTS 收缩候选仅小幅优于 Naive，可作为控制组和降级模型，不能替代申报创新模型 | P0 |
| GAN 鲁棒性增强 | 未实现 | 无 ACNN-LSTM/图频生成器、CNN 判别器、梯度惩罚、谱归一化和对抗训练报告 | P1 |
| 极端行情评价 | 部分诊断 | 已有收益分组，但缺少危机区间、波动冲击、缺失/噪声、分布漂移和压力测试协议 | P1 |
| LLM 解释生成 | 未实现 | SEP 仅源码归档/环境准备；无证据包、提示模板、解释数据集和可运行推理链路 | P1 |
| 自我反思与 PPO | 未实现 | 无偏好样本、奖励模型、PPO、KL 约束、LoRA 检查点和对照实验 | P1 |
| 完整基线矩阵 | 部分完成 | 缺 VAR、SVM、正式 RF、TCN、MTGNN/Graph WaveNet、TSGAN 和 SEP 统一金融数据对比 | P1 |
| 分类指标 | 部分完成 | 已有 Accuracy/F1，缺 Precision、Recall、MCC 和概率校准 | P1 |
| 投资绩效指标 | 未实现 | 缺交易信号协议、手续费、滑点、冲击成本、换手、容量、年化收益、夏普和最大回撤 | P0（商用） |
| 解释质量评价 | 未实现 | 缺忠实度、事实一致性、幻觉率、覆盖率、人工评分和用户调研 | P1 |
| 端到端系统 | 部分完成 | 有离线数据/训练/推理入口，但缺数据库、任务编排、版本 API、模型注册、在线服务和前后端联动 | P0（商用） |
| 安全与运维 | 未实现 | 缺身份认证、最小权限、密钥管理、审计日志、漂移监控、告警、备份、回滚和灾备 | P0（商用） |
| 合规与产品边界 | 未完成 | 尚未确定研究分析、投研辅助或投资决策系统的正式产品边界，也未完成法律和算法风险评审 | P0（商用） |
| 论文与知识产权 | 未开始正式产出 | 缺论文实验主表、创新性检索、论文初稿、软著代码鉴别材料和使用说明书 | P1 |

## 五、最重要的技术判断

### 5.1 阶段 D 候选应保留，但只能作为稳定控制组

`frets_return_l4__fixed_shrink_a075` 已通过冻结和未来独立评估，应继续作为：

- 新模型必须击败的强基线；
- 生产试运行时的降级模型；
- 图、多模态、GAN 和 LLM 模块的增量价值参照；
- 数据漂移或复杂模型失效时的安全回退。

不得把它改写成申报书核心创新已经完成，也不得重复读取 D-5 区间继续调参。

### 5.2 下一版图模型必须改变节点语义

下一版输入应为同一交易周的股票横截面面板：

```text
batch × time × stock_node × feature
```

图学习器在每个时间窗输出股票间邻接矩阵：

```text
batch × stock_node × stock_node
```

时序编码负责每只股票的历史，图传播负责同一时点股票间的信息传递。行业、产业链、收益相关性和文本相似度可作为先验图或边特征。确定性 Top-k 作为默认稳定方案，Gumbel 只作为预登记消融，不再作为默认生产路径。

### 5.3 多模态必须先解决数据覆盖和点时性

现有 18 条事件不足以支持 LLM 或多模态优势结论。必须先获得具有发布时间戳、股票映射和商业使用边界的公告/新闻/研报数据，并保证任何文本只能在其正式公开后进入特征。没有这一条件，不应继续增加复杂文本模型。

### 5.4 商用门槛不能用预测 MAE 代替

0.501% 的 MAE 改善是研究证据，不是商业价值证据。商用验收至少还需要成本后收益、容量、稳定性、延迟、可用性、安全、审计和合规同时通过。

## 六、修订后的阶段计划

### 阶段 E：申报主线重新对齐与横截面图模型（2026-07 至 2026-09）

目标：完成股票节点定义、扩大数据范围、补齐关键基线，并形成横截面动态图频 v1。

主要任务：

1. 冻结 `panel_v2` 数据契约，明确 `[时间, 股票, 特征]`、股票池变化、停牌、退市、复权和公司行动规则。
2. 先扩展至不少于 100 只股票、3 年数据完成工程试验，再以 300 只股票、5 年数据作为阶段验收目标。
3. 获取具有明确使用许可、发布时间和股票映射的公告/新闻文本；若无法取得商业许可，必须登记为商用阻塞项。
4. 实现股票横截面动态图、固定行业图、收益相关图、单位图和无图控制组。
5. 实现横截面 FFT 图传播，并与时域图、仅时域和 FreTS 收缩基线统一比较。
6. 补充 RF、SVM、TCN 和至少一个 MTGNN/Graph WaveNet 类强图基线；VAR 可在行业或因子层面实现。
7. 使用固定 rolling-origin、三种子、逐股票/行业/市值分组和最差折门槛，不读取 C-4、D-5 或未来 E-SCREENING。

阶段门槛：

- 图邻接矩阵确实表示股票/变量关系，并可导出审计；
- 至少 2/3 滚动折优于 FreTS 收缩和 Naive，最差折不得显著恶化；
- 三种子稳定性、股票覆盖率、行业分组和尾部误差同时通过；
- 多模态模型必须在同样本行上证明增量价值，否则降级为价格模型。

详细执行见 `plans/STAGE_E_APPLICATION_ALIGNMENT_PLAN.md`。

### 阶段 F：极端行情与 GAN 鲁棒性（2026-10 至 2026-11）

目标：兑现申报书第三阶段承诺，并证明对抗机制是否真的改善极端行情。

主要任务：

1. 预先定义高波动、连续下跌、跳空、流动性下降和数据噪声压力场景。
2. 先运行 Huber、尾部加权、噪声增强等非 GAN 稳健基线。
3. 实现 ACNN-LSTM/图频生成器、CNN 判别器、谱归一化和梯度惩罚。
4. 先预训练生成器，再进行受限对抗训练；记录判别器损失、梯度范数和模式坍塌检查。
5. 比较正常期、极端期和扰动数据上的 MAE、RMSE、方向、尾部损失及种子稳定性。

阶段门槛：极端组误差必须改善，正常组恶化不超过预登记上限，三个种子均无训练崩溃；若 GAN 不优于非 GAN 稳健损失，应保留负面结论而不强行进入主模型。

### 阶段 G：LLM 解释、自我反思与偏好优化（2026-12 至 2027-01）

目标：让解释严格引用结构化证据，并完成申报书 LLM 路线的可核验实验。

主要任务：

1. 构建解释证据包：预测、历史特征、文本事件、重要邻边、频率成分、组件分歧和不确定性。
2. 建立规则模板与检索增强解释基线，作为 LLM 的事实一致性控制组。
3. 选择可本地部署的开源模型，完成 LoRA/SFT；生成初始解释、错误反馈和自我反思修订样本。
4. 构建成对偏好数据；先进行 DPO 或奖励模型验证，再按申报书完成小规模 PPO 与 KL 约束实验。
5. 评价事实一致性、证据覆盖、忠实度、幻觉率、稳定性和人工评分。

阶段门槛：关键数值和事件引用正确率达到预登记标准，禁止生成未经证据支持的因果解释、收益保证或直接交易建议。

### 阶段 H：端到端系统与商用工程（2027-02 至 2027-04）

目标：完成申报书系统集成，并把研究代码提升为可受控试运行的工程系统。

主要任务：

1. 建立增量数据任务、特征存储、模型注册、批量推理和版本化 REST API。
2. 实现容器化、自动测试、环境隔离、幂等任务、超时重试、限流、降级和一键回滚。
3. 增加身份认证、最小权限、密钥管理、数据脱敏、操作审计和依赖安全扫描。
4. 监控数据质量、特征漂移、预测漂移、误差、覆盖率、延迟、吞吐和资源使用。
5. 建立含手续费、滑点、冲击成本、换手和容量限制的业务回测；输出年化收益、夏普和最大回撤，但不作收益承诺。
6. 将 dashboard 改为读取正式 API，并显示模型版本、数据时间、风险边界和审计编号。

阶段门槛：功能、性能、安全、恢复、监控和成本后业务指标全部形成可复算验收记录；未通过合规审查前不得进入真实交易链路。

### 阶段 I：独立验收、论文与知识产权（2027-05 至 2027-06）

目标：完成一次新的独立评价、项目结题材料和成果申报。

主要任务：

1. 在候选、数据范围、指标和失败条件冻结后，申请新的未来 SCREENING/FINAL 数据授权。
2. 由独立入口复算模型、基线、解释和业务指标，禁止在该区间重新选模或调参。
3. 完成论文实验主表、消融、稳健性、失败案例和局限性论述。
4. 准备软件著作权源程序鉴别材料、用户手册；评估频域横截面图学习是否具备专利申请价值。
5. 汇总经费、会议记录、版本标签、SHA-256 清单、数据授权和验收报告。

阶段门槛：申报书承诺项、学校结题要求和商用验收清单逐项关闭；任何未满足项目必须在最终报告中明确列为限制，不能用展示页面替代技术证据。

## 七、未来四周立即执行顺序

| 周次 | 工作 | 输出 |
|---|---|---|
| 第 1 周 | 冻结 Stage E 数据契约、股票节点语义、数据隔离和基线列表 | `panel_v2` 契约、协议 JSON、测试 |
| 第 2 周 | 构建 100 股票横截面面板和固定行业/相关性图 | 数据质量报告、图统计、无泄漏检查 |
| 第 3 周 | 实现横截面动态图频 v1 和统一适配器 | 模型代码、最小过拟合测试、邻接矩阵导出 |
| 第 4 周 | 运行三折三种子受限比较和无图/固定图/动态图/无频域消融 | Stage E 初始报告、失败归因、唯一下一步建议 |

四周内不得进行的事项：读取新的未来 SCREENING、在同一验证集无限增加候选、直接开始大规模 PPO、把 D-5 的 0.501% 改善表述为商业收益，或在没有数据许可的情况下把 CSMAR 数据用于商业部署。

## 八、成果验收清单

最终结题至少需要以下可核验证据：

- 数据来源、许可、点时规则、质量报告和 SHA-256 追溯；
- 股票横截面动态图与图频模型源码、配置、测试和邻接解释；
- 完整基线、三种子、滚动验证、独立评估、消融和显著性分析；
- GAN 与非 GAN 鲁棒性对照及极端行情报告；
- 多模态融合对照、LLM 解释数据、自我反思/PPO 实验和解释质量报告；
- 交易成本后业务评价、安全合规边界和失败条件；
- 独立加载推理、模型注册、API、监控、告警、回滚和审计记录；
- 论文初稿、软件著作权或专利材料、用户手册和最终项目报告。

## 九、最终判断

阶段 A—D 不是无效工作，而是为申报项目建立了少见的可复现、可冻结、可独立评估基础。当前最大的风险不是工程质量，而是研究主线偏离：稳定通过的模型仍是 FreTS 基线，横截面动态图、多模态、GAN 和 LLM 尚未成为经验证的完整系统。

因此下一阶段的唯一正确方向是：保留 Stage D 候选作为稳定控制和降级方案，以真正的股票横截面节点重新实现图频主模型，同时并行解决文本数据授权和覆盖问题；只有该核心主线稳定后，才进入 GAN、LLM 和生产系统集成。
